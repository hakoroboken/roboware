## Smart Controller Gate Way（スマホアプリとの接続部）
**scgw.param.yaml**です。
```yaml
/**:
  ros__parameters:
    port: 64201
    debug: false
```
上から

**port** :　ポート番号を指定します

**debug** :　デバッグモードなのかそうじゃないのかを指定します

